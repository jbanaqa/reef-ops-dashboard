/* eslint-disable react/no-unescaped-entities */  
"use client";
import { useMemo, useState } from "react";
import { Content, defaultContent, render } from "@/lib/marketing/rules";
import { FlowMap, Node, flowNodes } from "./FlowMap";
type Step={minutes:number;subject:string;channel:string;content:Content};type Branch={subject:string;content:Content};type Data={reviewed?:boolean;description?:string;steps:Step[];smsContent?:Content;orderBranch?:{yes:Branch;no:Branch}};type Resource={key:string;name:string;enabled:boolean;data:Record<string,unknown>};type Target={kind:"step"|"sms"|"branch"|"wait"|"info";index?:number;branch?:"yes"|"no"};
export default function FlowEditor({resource,busy,save}:{resource:Resource;busy:boolean;save:(data:Data,enabled:boolean)=>void}){const initial=resource.data as unknown as Data;const[flow,setFlow]=useState<Data>(()=>JSON.parse(JSON.stringify(initial)));const[enabled,setEnabled]=useState(resource.enabled);const[selected,setSelected]=useState<{node:Node;target:Target}|null>(null);const nodes=useMemo(()=>flowNodes(resource),[resource]);const targetFor=(n:Node):Target=>{if(n.kind==="wait")return{kind:"wait",index:nodes.slice(0,nodes.indexOf(n)).filter(x=>x.kind==="wait").length};if(n.kind==="sms")return{kind:"sms"};if(n.kind!=="email")return{kind:"info"};if(n.label.includes("Another Soft Push"))return{kind:"branch",branch:"yes"};if(n.label.includes("Discount Offer"))return{kind:"branch",branch:"no"};return{kind:"step",index:Math.min(nodes.slice(0,nodes.indexOf(n)).filter(x=>x.kind==="email").length,Math.max(0,flow.steps.length-1))}};const updateStep=(i:number,p:Partial<Step>)=>setFlow(f=>({...f,steps:f.steps.map((s,n)=>n===i?{...s,...p}:s)}));const updateContent=(t:Target,k:keyof Content,v:string)=>setFlow(f=>t.kind==="sms"?{...f,smsContent:{...(f.smsContent||defaultContent),[k]:v}}:t.kind==="step"?{...f,steps:f.steps.map((s,i)=>i===t.index?{...s,content:{...s.content,[k]:v}}:s)}:t.kind==="branch"&&f.orderBranch&&t.branch?{...f,orderBranch:{...f.orderBranch,[t.branch]:{...f.orderBranch[t.branch],content:{...f.orderBranch[t.branch].content,[k]:v}}}}:f);const content=selected?.target.kind==="sms"?(flow.smsContent||defaultContent):selected?.target.kind==="step"?flow.steps[selected.target.index||0]?.content:selected?.target.kind==="branch"&&selected.target.branch?flow.orderBranch?.[selected.target.branch]?.content:null;const step=selected?.target.kind==="step"?flow.steps[selected.target.index||0]:null;const branch=selected?.target.kind==="branch"&&selected.target.branch?flow.orderBranch?.[selected.target.branch]:null;const preview=content?render(content,"#unsubscribe","Your business mailing address"):"";const fields=(c:Content,t:Target)=><>{(t.kind==="step"||t.kind==="branch")&&<label>Subject<input value={(t.kind==="step"?step?.subject:branch?.subject)||""} onChange={e=>t.kind==="step"?updateStep(t.index||0,{subject:e.target.value}):t.kind==="branch"&&setFlow(f=>({...f,orderBranch:{...f.orderBranch!,[t.branch!]:{...f.orderBranch![t.branch!],subject:e.target.value}}}))}/></label>}<label>Heading<input value={c.heading||""} onChange={e=>updateContent(t,"heading",e.target.value)}/></label><label>Message<textarea rows={5} value={c.body||""} onChange={e=>updateContent(t,"body",e.target.value)}/></label><div className="mk-two"><label>Button text<input value={c.button||""} onChange={e=>updateContent(t,"button",e.target.value)}/></label><label>Button destination<input value={c.url||""} onChange={e=>updateContent(t,"url",e.target.value)}/></label></div></>;return <article className="mk-panel"><div className="mk-flow-editor-heading"><div><h2>Edit {resource.name}</h2><p>{flow.description}</p></div><span className="mk-flow-editor-hint">Click any step to edit it</span></div><div className="mk-flow-note"><strong>How this works</strong><span>Follow the timeline from top to bottom. Click a block to view or edit its settings.</span></div><FlowMap resource={resource} onNodeClick={n=>setSelected({node:n,target:targetFor(n)})}/><div className="mk-flow-controls"><label className="mk-check"><input type="checkbox" checked={!!flow.reviewed} onChange={e=>setFlow({...flow,reviewed:e.target.checked})}/>I reviewed this flow's timing, consent rules, and purchase checks.</label><label className="mk-check"><input type="checkbox" checked={enabled} onChange={e=>setEnabled(e.target.checked)}/>Enable this flow</label><button disabled={busy} onClick={()=>save(flow,enabled)}>Save flow</button></div>{selected&&<div className="mk-modal-backdrop" onClick={()=>setSelected(null)}><div className="mk-modal" role="dialog" aria-modal="true" onClick={e=>e.stopPropagation()}><div className="mk-modal-header"><h3>{selected.node.label}</h3><button type="button" onClick={()=>setSelected(null)}>Close</button></div>{selected.target.kind==="info"&&<p className="mk-modal-explanation">{selected.node.detail||"Reef Ops evaluates this decision automatically before continuing."}</p>}{selected.target.kind==="wait"&&<><p className="mk-modal-explanation">This pause gives the customer time before the next action.</p><label>Wait (minutes)<input type="number" min="0" value={step?.minutes||0} onChange={e=>updateStep(selected.target.index||0,{minutes:Number(e.target.value)})}/></label></>}{content&&fields(content,selected.target)}{content?.template==="b2b-wholesale"&&<div className="mk-image-fields"><p className="mk-modal-label">Brand images</p><label>Logo image<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" onChange={e=>{const file=e.target.files?.[0];if(file){const reader=new FileReader();reader.onload=()=>updateContent(selected.target,"logo",String(reader.result));reader.readAsDataURL(file)}}}/></label><label>Footer image<input type="file" accept="image/png,image/jpeg,image/webp,image/gif" onChange={e=>{const file=e.target.files?.[0];if(file){const reader=new FileReader();reader.onload=()=>updateContent(selected.target,"footerImage",String(reader.result));reader.readAsDataURL(file)}}}/></label><p className="mk-modal-explanation">Placeholders appear until you upload the matching Klaviyo artwork.</p></div>}{preview&&<div className="mk-email-preview"><p className="mk-modal-label">Live email preview</p><iframe title="Email preview" sandbox="" srcDoc={preview}/></div>}{selected.target.kind==="sms"&&<p className="mk-modal-explanation">SMS requires marketing consent and an unsuppressed profile.</p>}<div className="mk-modal-actions"><button type="button" onClick={()=>setSelected(null)}>Done</button></div></div></div>}</article>}